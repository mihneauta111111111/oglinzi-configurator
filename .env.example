# Base URL of the WordPress/WooCommerce backend (no trailing slash).
# For local dev (Local by WP Engine), use http://halomirrors.local
# For production, once WordPress is hosted live, use https://halomirrors.ro
VITE_API_URL=https://halomirrors.ro
